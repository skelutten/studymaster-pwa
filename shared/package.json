{
  "name": "studymaster-shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for StudyMaster",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "rimraf": "^5.0.1"
  }
}