# Production Environment Variables
# Copy this file to .env.production.local and update with your production values

# PocketBase Configuration (REQUIRED)
VITE_POCKETBASE_URL=https://your-production-pocketbase.com

# Application Configuration
NODE_ENV=production
VITE_APP_NAME=StudyMaster
VITE_APP_VERSION=1.0.0

# Security Configuration  
VITE_ENABLE_DEBUG_LOGGING=false

# DO NOT SET DEMO CREDENTIALS IN PRODUCTION
# Demo authentication is automatically disabled in production builds

# Deployment Instructions:
# 1. Set VITE_POCKETBASE_URL in Vercel dashboard environment variables
# 2. Ensure NODE_ENV=production is set
# 3. Do not include any demo user credentials
# 4. Verify all environment variables are properly configured before deployment